<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sports Play</title>
<head>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet"></link>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/1000hz-bootstrap-validator/0.11.9/validator.min.js"></script>
    <style>
      #createSportBtns {
        background: #2E8B57;
        font-weight: bold;
        color: white;
        box-shadow: 0px 3px 3px 0px #2E8B57 ;
        width: 100%;
        height: 5%;
        font-size: 14px;
        border: none;
      }
      
    #createSportBtns:hover {
      background: #2E8B57;
          }
    
        body {
          height: 100vh;
          width: 100%;
          background-position: center;
          background: #BC8F8F;
          background-size: cover;
        }
        #sportsNav {
         display: inline-flex;
         color: white;
       }
       #sportsNav li {
         padding: 6px;
         list-style: none;
         cursor: pointer;
         margin-left: 16px;
       }
       #sportsNav li:hover {
         background-color: white;
         transition: 0.6s;
         color: #BC8F8F;
         border-radius: 30px;
         font-weight: bold;
       }
       #sportsNav li a {
         text-decoration: none;
       }
        .container {
            max-width: 550px;
        }
        .has-error input {
            color: red;
            border-color: red;
        }
        .list-unstyled li {
            font-size: 12px;
            color: red;
            padding: 4px 0 0;
        }
    </style>
</head>
<body>
  

  <section style="width:100%; justify-content: center; height:100vh;display: flex; align-items: center;">

    <div class="container mt-5">
        <div class="card">
            <h5 class="card-header text-center">Reset your password here!</h5>
            <div class="card-body">
              <form action="/reset" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    
                    <div class="form-group">
                        <label>Old Password</label>
                        <input  type="password" name="oldPassword" class="form-control" name="sports_name"  id="inputName" placeholder="Enter your old password">
                        <!-- Error -->
                        <div class="help-block with-errors" style="color: red;">
                          <% if(messages && messages.error && messages.error.length > 0)  { %>
                            <% for(var i=0; i<messages.error.length; i++) { %>
                              <p class="bg-red-200 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
                            <% } %>
                          <% } %>
                        </div>
                    </div>
                    <div class="form-group">
                      <label>New Password</label>
                      <input type="password" name="newPassword" class="form-control"  placeholder="Enter the new password">
                  </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary btn-block">Reset</button>
                    </div>
                    <div class="form-group">
                        <button type="button" class="btn btn-outline-danger btn-block" onclick="window.location.href='/all_sports'">Back</button>
                    </div>
                    <% if(messages && messages.error && messages.error.length > 0)  { %>
                      <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-red-200 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
                      <% } %>
                    <% } %>
                </form>
            </div>
        </div>
    </div>
    </section>
</body>
</html>
