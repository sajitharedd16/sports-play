<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sports play</title>
<head>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet"></link>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/1000hz-bootstrap-validator/0.11.9/validator.min.js"></script>
    <style>
      #createSportBtns {
        background: #FFD700;
        font-weight: bold;
        box-shadow: 0px 3px 3px 0px rgba(0, 0, 0, 0.25);
        color: white;
        height: 7%;
        font-size: 13px;
        width: 100%;
        border: none;
      }
      
    #createSportBtns:hover {
      background: #FFD700;
          }
    
        body {
          height: 100vh;
          width: 100%;
          background-size: cover;
          background: linear-gradient(rgba(0,0,0,.3), rgba(0,0,0, 1)), url('./bg1.jpg');
          background-position: center;
        }
        #sportsNav {
         display: inline-flex;
         color: white;
       }
       #sportsNav li {
         list-style: none;
         padding: 6px;
         margin-left: 16px;
         cursor: pointer;
       }
       #sportsNav li:hover {
         background-color: white;
         border-radius: 32px;
         color: black;
         font-weight: bold;
         transition: 0.7s;
       }
       #sportsNav li a {
         text-decoration: none;
       }
        .container {
            max-width: 550px;
            
        }
        
        .has-error input {
            color: red;
            border-color: #D8BFD8;
        }
        .list-unstyled li {
            font-size: 13px;
            padding: 4px 0 0;
            color: ;
        }
    </style>
</head>
<body>
  

  <section style="width:100%; height:100vh;display: flex; justify-content: center; align-items: center;">

    <div class="container mt-5">
        <div class="card">
            <h5 class="card-header text-center">Reset Your Password Here!</h5>
            <div class="card-body">
              <form action="/reset" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
    
                    <div class="form-group">
                        <label>Old password</label>
                        <input  type="password" name="oldPassword" class="form-control" name="sports_name"  id="inputName" placeholder="Enter your old password">
                        <!-- Error -->
                        <div class="help-block with-errors" style="color: red;">
                          <% if(messages && messages.error && messages.error.length > 0)  { %>
                            <% for(var i=0; i<messages.error.length; i++) { %>
                              <p class="bg-red-200 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
                            <% } %>
                          <% } %>
                        </div>
                    </div>
                    <div class="form-group">
                      <label>New password</label>
                      <input type="password" name="newPassword" class="form-control"  placeholder="Enter your new password">
                  </div>
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary btn-block">Reset</button>
                    </div>
                    <div class="form-group">
                        <button type="button" class="btn btn-outline-danger btn-block" onclick="window.location.href='/all_sports'">Back</button>
                    </div>
                    <% if(messages && messages.error && messages.error.length > 0)  { %>
                      <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-red-200 my-3 list-none rounded px-4 py-2"><%= messages.error[i] %></p>
                      <% } %>
                    <% } %>
                </form>
            </div>
        </div>
    </div>
    </section>
</body>
</html>
