<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="csrf-token" content="<%= csrfToken %>">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sports Play</title>

  <head>
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    </link>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/1000hz-bootstrap-validator/0.11.9/validator.min.js"></script>
    <style>
      body {
        height: 100vh;
        width: 100%;
        background-position: center;
        background: Plum;
        background-size: cover;
      }
      #sportsNav {
        color: white;
        display: inline-flex;
      }
      #sportsNav li {
        list-style: none;
        cursor: pointer;
        margin-left: 16px;
        padding: 6px;
      }
      #sportsNav li a {
        text-decoration: none;
      }
      #sportsNav li:hover {
        background-color: white;
        font-weight: bold;
        color: black;
        border-radius: 30px;
        transition: 0.6s;
      }
      .container {
        max-width: 550px;
      }
      .has-error input {
        color: red;
        border-color: red;
      }
      .list-unstyled li {
        font-size: 12px;
        color: red;
        padding: 3px 0 0;
      }
    </style>
  </head>

<body>
  <!-- <nav class="navbar" style="background-color: black; position: sticky">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" style="color:RosyBrown">Sports Play</a>
      <ul id="sportsNav">
        <li>Sign out</li>
      </ul>
    </div>
  </nav> -->
  <section style="width:100%; justify-content: center; align-items: center; height:100vh;display: flex; ">

    <div class="container mt-5">
      <div class="card">
        <h5 class="card-header text-center">Add New Sport Here<h5>
            <div class="card-body">
              <form action="/admin_add_sports" method="post">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>" />
                <input type="hidden" name="update_sports_name" value="<%= update_sports_name %>" />
                <input type="hidden" name="update_sports_id" value="<%= update_sports_id %>" />
                <div class="form-group">
                  <label>Enter your new sport name</label>
                  <input type="text" class="form-control" name="sports_name" id="inputName"
                    placeholder="Enter Sports Name">
                  <!-- Error -->
                  <div class="help-block with-errors" style="color: red;">
                    <% if(messages && messages.error && messages.error.length> 0) { %>
                      <% for(var i=0; i<messages.error.length; i++) { %>
                        <p class="bg-red-200 my-3 list-none rounded px-4 py-2">
                          <%= messages.error[i] %>
                        </p>
                        <% } %>
                          <% } %>
                  </div>
                </div>
                <div class="form-group">
                  <button type="submit" class="btn btn-primary btn-block">Create</button>
                </div>
                <div class="form-group">
                  <button type="button" class="btn btn-outline-danger btn-block" onclick="window.location.href='/all_sports'">Back</button>
                </div>
              </form>
            </div>
      </div>
    </div>
  </section>
</body>

</html>
